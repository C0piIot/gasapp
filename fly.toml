app = "gasapp"
primary_region = "cdg"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]

[http_service]
  internal_port = 8080
  force_https = true

[[mounts]]
  source = "data"
  destination = "/data"

[[statics]]
  guest_path = "/app/static"
  url_prefix = "/static"
