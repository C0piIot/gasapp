app = "gasapp"
primary_region = "cdg"
kill_signal = "SIGINT"
kill_timeout = "5s"

[build]

[[mounts]]
  source = "gasapp_data"
  destination = "/app/data"

[http_service]
  internal_port = 8080
  force_https = true

[[statics]]
  guest_path = "/app/static"
  url_prefix = "/static"
